<%- include('partials/header') %>

<main>
    <h1><%= title %></h1>

    <h2>Dashboard</h2>
    <p>Name: <%= name %></p>
    <p>Username: <%= email %></p>

    <% if (user.role_name == 'admin') { %>
    <p><a href="/secret-route">Secret Route</a></p>
    <% } %>

</main>

<%- include('partials/footer') %>